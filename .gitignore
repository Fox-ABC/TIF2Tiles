# 忽略匹配下列规则的Git 提交 V2.1.0
### gradle ###
.gradle
/build/
!gradle/wrapper/gradle-wrapper.jar

### STS ###
.settings/
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
bin/

### VSCode ###
.vscode/

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr
# 禁止写全局 *.lock：会误忽略 src-tauri/Cargo.lock（Tauri 应用应提交 Cargo.lock 以锁定 Rust 依赖）
rebel.xml

### NetBeans ###
nbproject/private/
build/
nbbuild/
nbdist/
.nb-gradle/

### maven ###
target/
*.war
*.ear
*.zip
*.tar
*.tar.gz

### logs ####
/logs/
*.log

### temp ignore ###
*.cache
*.diff
*.patch
*.tmp
*.java~
*.properties~
*.xml~
sessionStore

### system ignore ###
.DS_Store
Thumbs.db
Servers
.metadata
upload
gen_code
application-preview.yml

### node ###
node_modules

### 本地临时 / 历史离线构建残留（不入库） ###
.tmp/
.offline/
vendor/
offline-dev-kit/

### OSS 内部凭证（从 oss-keys.local.example.ts 复制，勿提交） ###
src/config/oss-keys.local.ts

### Tauri / Vite（构建产物与缓存，勿提交；与 tauri.conf 的 frontendDist 一致） ###
/dist/
# 历史/备份构建目录（dist_new、dist_old、dist_old2 等）
/dist_*/
# Vite 缓存目录
.vite/

# GDAL 等随包分发的原生库体积大，本地或 CI 放置即可；勿入库（README 可单独保留在 gdal/ 下）
src-tauri/resources/gdal/lib/

### 环境变量（本地密钥，勿提交） ###
.env
.env.local
.env.*.local

### test
*-aitest*